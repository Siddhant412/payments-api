apiVersion: apps/v1
kind: Deployment
metadata:
  name: payments-api
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: payments-api
          env:
            - name: NODE_ENV
              value: development
