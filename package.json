{
  "name": "payments-api",
  "version": "0.1.0",
  "private": true,
  "description": "API for payment operations",
  "type": "module",
  "engines": {
    "node": "22 || 24"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "lint": "node --check src/index.js && node --check src/server.js"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  }
}
